/**
 * Shared Services
 * Re-exports all shared services for easy importing
 *
 * Note: Most services have been moved to lib/utils or removed
 * - Response builders: use lib/utils/response-builder
 * - API endpoints: use lib/config/api-endpoints
 * - Cache: TanStack Query handles client-side caching
 */
