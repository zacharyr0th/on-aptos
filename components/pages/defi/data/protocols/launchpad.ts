import type { DefiProtocol } from "../types";
import { LaunchpadImplementation } from "../types";

// Launchpad protocols
export const launchpadProtocols: DefiProtocol[] = [
  // No launchpad protocols currently supported
];
