'use client';

// This component has been simplified for the enterprise design
// Background patterns are now handled by CSS classes in the main layout

export const FloatingOrbs = () => {
  return null;
};